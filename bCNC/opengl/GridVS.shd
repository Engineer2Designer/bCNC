// GLSL ES 1.00
#version 100
attribute vec3 xyz;
attribute float position;

varying vec3 color;
varying float position_;

uniform mat4 MVP;

void main() {
    gl_Position = MVP * vec4(xyz, 1);

    position_ = position;
}