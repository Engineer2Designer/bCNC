// GLSL ES 1.00
#version 100
attribute vec3 xyz;
attribute float position;
attribute float axis; // 1, 2, 3 for x, y, z

varying vec3 color;
varying float position_;

uniform mat4 MVP;

void main() {
    gl_Position = MVP * vec4(xyz, 1);

    position_ = position;
    
    // Color per axis
    if (axis == 1.0)
    {
        color = vec3(1, 0, 0);
    }
    else if (axis == 2.0)
    {
        color = vec3(0, 1, 0);
    }
    else if (axis == 3.0)
    {
        color = vec3(0, 0, 1);
    }
}