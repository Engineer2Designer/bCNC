// GLSL ES 1.00
#version 100
precision highp float;  // Required in fragment shader

varying vec2 texCoords;

uniform sampler2D ourTexture;

void main() {
    gl_FragColor = texture2D(ourTexture, texCoords);
}