// GLSL ES 1.00
#version 100
precision highp float;  // Required in fragment shader

varying vec3 color;     // Color passed from vertex shader
uniform float alpha; // Opacity

void main() {
    gl_FragColor = vec4(color, alpha);
}